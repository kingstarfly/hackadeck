# Convex Setup

This folder is ready for the realtime HackaDeck backend.

Suggested next steps:

1. Run `pnpm dlx convex dev`.
2. Add `NEXT_PUBLIC_CONVEX_URL` to `.env.local`.
3. Add mutations for participant creation, card run creation, and gallery queries.
4. Add actions for GPT-5.5 card specs and GPT Image 2 familiar art.

For the hackathon shortcut, participant identity is the submitted email address.
